export type EditRoomType = {
  roomAuthorId: string
  roomHostDay: number[]
  roomEndTime: number[]
  roomMeetTitle: string
  roomMeetType: string
  roomMeetMessage: string
  roomStartTime: number[]
  roomId: string
}
